{{#if isChairPosition }}
    {{#if panel.chair }}
        {{model/adjudicator-ui adj=panel.chair}}
    {{/if }}
{{/if }}

{{#if isPanellistsPosition}}
    {{#if panel.panellists }}
    {{#each panel.panellists as |panellist|}}
        {{model/adjudicator-ui adj=panellist}}
    {{/each}}
    {{/if }}
{{/if }}

{{#if isTraineesPosition }}
    {{#if panel.trainees }}
    {{#each panel.trainees as |trainee|}}
        {{model/adjudicator-ui adj=trainee}}
    {{/each}}
    {{/if }}
{{/if }}
